<UserControl
    x:Class="Automation.App.Components.NodeIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Automation.App.Components"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance local:NodeIcon}">
    <TextBlock
        FontFamily="{StaticResource FontAwesomeSolid}"
        VerticalAlignment="Center"
        DataContext="{Binding Type, RelativeSource={RelativeSource AncestorType=local:NodeIcon}}">
        <TextBlock.Style>
            <Style TargetType="TextBlock">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding}" Value="Scope">
                        <Setter Property="Text" Value="{x:Static local:IconFont.Folder}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding}" Value="Workflow">
                        <Setter Property="Text" Value="{x:Static local:IconFont.Cubes}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding}" Value="Task">
                        <Setter Property="Text" Value="{x:Static local:IconFont.Cube}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </TextBlock.Style>
    </TextBlock>
</UserControl>
