<UserControl
    x:Class="Automation.App.Components.ScopedElementIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Automation.App.Components"
    xmlns:base="clr-namespace:Automation.Base;assembly=Automation.Base"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance local:ScopedElementIcon}">
    <TextBlock
        FontFamily="{StaticResource FontAwesomeSolid}"
        VerticalAlignment="Center"
        DataContext="{Binding Type, RelativeSource={RelativeSource AncestorType=local:ScopedElementIcon}}">
        <TextBlock.Style>
            <Style TargetType="TextBlock">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding}" Value="Scope">
                        <Setter Property="Text" Value="&#xf07b;" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding}" Value="Workflow">
                        <Setter Property="Text" Value="&#xf1b3;" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding}" Value="Task">
                        <Setter Property="Text" Value="&#xf1b2;" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </TextBlock.Style>
    </TextBlock>
</UserControl>
