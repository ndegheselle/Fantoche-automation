<UserControl
    x:Class="Automation.App.Components.Display.SelectIconModal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Automation.App.Components.Display"
    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
    xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
    xmlns:fonts="clr-namespace:Joufflu.Shared.Resources.Fonts;assembly=Joufflu.Shared"
    mc:Ignorable="d"
    MinHeight="200"
    MinWidth="400"
    x:Name="ContainerElement">
    <Grid DataContext="{Binding ElementName=ContainerElement}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}" />
            <fonts:Icon
                Margin="8,0"
                HorizontalAlignment="Right"
                Text="{x:Static fonts:IconFont.MagnifyingGlass}"
                Foreground="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}" />
        </Grid>

        <ListBox ItemsSource="{Binding IconsView}" Margin="0,2,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <fonts:Icon Text="{Binding Icon}" />
                        <TextBlock Text="{Binding BindingGroupName}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border
            Margin="0,2,0,0"
            Padding="2"
            Grid.Row="2"
            Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}">
            <Button
                adonisExtensions:LayerExtension.Layer="2"
                Command="{Binding SelectCommand}"
                HorizontalAlignment="Right"
                Padding="20,4">
                Select
            </Button>
        </Border>
    </Grid>
</UserControl>
