<UserControl
    x:Class="Automation.App.Components.Data.ContextEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Automation.App.Components.Data"
    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
    mc:Ignorable="d"
    MinHeight="100"
    MinWidth="250"
    d:DataContext="{d:DesignInstance local:DataNode}">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:DataNode}">
            <StackPanel>
                <TextBlock Margin="0,4">Key</TextBlock>
                <TextBox Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:DataValue}">
            <StackPanel>
                <TextBlock Margin="0,4">Key</TextBlock>
                <TextBox Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Margin="0,4">Value</TextBlock>
                <TextBox Text="{Binding Value, Mode=TwoWay}" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <ContentControl Content="{Binding}" Margin="4" />
</UserControl>
