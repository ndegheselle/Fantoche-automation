<UserControl x:Class="Automation.App.Views.WorkPages.Workflows.Editor.Components.ContextMapping"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fonts="clr-namespace:Joufflu.Shared.Resources.Fonts;assembly=Joufflu.Shared"
             xmlns:converters="clr-namespace:Joufflu.Shared.Converters;assembly=Joufflu.Shared"
             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
             mc:Ignorable="d"
             x:Name="ContainerElement"
             d:DesignHeight="400" d:DesignWidth="400">
    <UserControl.Resources>
        <Style
            x:Key="CodeTextBox"
            TargetType="TextBox"
            BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="AcceptsTab" Value="True" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <converters:BooleanConverter x:Key="BooleanConverter" />
    </UserControl.Resources>
    <Grid DataContext="{Binding ElementName=ContainerElement}" Margin="2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid Width="170">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Inputs, Converter={StaticResource BooleanConverter}}"
                                     Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <fonts:Icon Text="{x:Static fonts:IconFont.FileArrowDown}" />
                <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">Input sample</TextBlock>
            </StackPanel>

            <ItemsControl
                Grid.Row="1"
                ItemsSource="{Binding Inputs}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBox Margin="0,0,0,2"
                                 Text="{Binding Mode=OneTime }" Style="{StaticResource CodeTextBox}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <Grid Grid.Column="1" Margin="2,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="6" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <fonts:Icon Text="{x:Static fonts:IconFont.File}" />
                    <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">Settings</TextBlock>
                </StackPanel>
                <TextBox
                    Grid.Row="1"
                    Style="{StaticResource CodeTextBox}"
                    Text="{Binding SettingsJson, Mode=TwoWay}"
/>
            </Grid>

            <GridSplitter
                Padding="0,2"
                Grid.Row="1"
                Background="{StaticResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                ShowsPreview="True"
                Height="2" />

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <fonts:Icon Text="{x:Static fonts:IconFont.FileDashed}" />
                    <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">Schema</TextBlock>
                </StackPanel>
                <TextBox
                    Grid.Row="1"
                    Style="{StaticResource CodeTextBox}"
                    Text="{Binding SchemaJson, Mode=TwoWay}"
                    IsReadOnly="True" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>