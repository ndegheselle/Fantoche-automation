<UserControl x:Class="Automation.App.Views.WorkPages.Workflows.Editor.Components.SchemaDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI" 
             xmlns:fonts="clr-namespace:Joufflu.Shared.Resources.Fonts;assembly=Joufflu.Shared"
             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
             mc:Ignorable="d"
             x:Name="ContainerElement"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style
            x:Key="CodeTextBox"
            TargetType="TextBox"
            BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="AcceptsTab" Value="True" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
    </UserControl.Resources>
    <TabControl DataContext="{Binding ElementName=ContainerElement}">
        <TabItem Padding="4,0">
            <TabItem.Header>
                <StackPanel Orientation="Horizontal">
                    <fonts:Icon Text="{x:Static fonts:IconFont.FileDotted}" />
                    <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">Schema</TextBlock>
                </StackPanel>
            </TabItem.Header>
            <TextBox Margin="0,2,0,0" IsReadOnly="{Binding IsReadOnly}"
                     Text="{Binding SchemaJson, UpdateSourceTrigger=PropertyChanged}"
                     TextChanged="OnSchemaJsonChanged"
                     adonisExtensions:CursorSpotlightExtension.BorderBrush="Transparent">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource CodeTextBox}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WithError}" Value="True">
                                <Setter Property="BorderBrush"
                                        Value="{StaticResource {x:Static adonisUi:Brushes.ErrorBrush}}" />
                                <Setter Property="BorderThickness" Value="2" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </TabItem>
        <TabItem Padding="4,0">
            <TabItem.Header>
                <StackPanel Orientation="Horizontal">
                    <fonts:Icon Text="{x:Static fonts:IconFont.File}" />
                    <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">Sample</TextBlock>
                </StackPanel>
            </TabItem.Header>
            <TextBox Margin="0,2,0,0" IsReadOnly="{Binding IsReadOnly}"
                     Text="{Binding SampleJson, UpdateSourceTrigger=PropertyChanged}"
                     TextChanged="OnSampleJsonChanged"
                     adonisExtensions:CursorSpotlightExtension.BorderBrush="Transparent">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource CodeTextBox}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WithError}" Value="True">
                                <Setter Property="BorderBrush"
                                        Value="{StaticResource {x:Static adonisUi:Brushes.ErrorBrush}}" />
                                <Setter Property="BorderThickness" Value="2" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </TabItem>
    </TabControl>
</UserControl>