<UserControl
    x:Class="Automation.App.Features.Workflows.Details.Controls.MetadataEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Automation.App.Features.Workflows.Details.Controls"
    xmlns:converters="clr-namespace:Automation.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:inputs="clr-namespace:Joufflu.Inputs.Controls;assembly=Joufflu.Inputs"
    xmlns:jonverters="clr-namespace:Joufflu.Converters;assembly=Joufflu"
    xmlns:joufflu="clr-namespace:Joufflu;assembly=Joufflu"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:scoped="clr-namespace:Automation.Shared.Data.Scoped;assembly=Automation.Shared"
    d:DataContext="{d:DesignInstance Type=scoped:ScopedMetadata}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:HexColorConverter x:Key="HexColorConverter" />
    </UserControl.Resources>
    <Grid joufflu:Spacing.Gap="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBlock Style="{StaticResource Muted}">Name</TextBlock>
            <TextBox IsEnabled="{Binding IsReadOnly, Converter={x:Static jonverters:BooleanFlipConverter.Default}, Mode=OneTime}" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel
            Grid.Row="0"
            Grid.Column="1"
            Margin="4,0,0,0">
            <TextBlock Style="{StaticResource Muted}">Icon</TextBlock>
            <controls:IconPicker IsEnabled="{Binding IsReadOnly, Converter={x:Static jonverters:BooleanFlipConverter.Default}, Mode=OneTime}" SelectedIcon="{Binding Icon, Mode=TwoWay}" />
        </StackPanel>

        <StackPanel
            Grid.Row="0"
            Grid.Column="2"
            Margin="4,0,0,0">
            <TextBlock Style="{StaticResource Muted}">Color</TextBlock>
            <inputs:ColorPicker
                Width="140"
                IsEnabled="{Binding IsReadOnly, Converter={x:Static jonverters:BooleanFlipConverter.Default}, Mode=OneTime}"
                Color="{Binding Color, Mode=TwoWay, Converter={StaticResource HexColorConverter}}" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Margin="0,4,0,0">
            <TextBlock Style="{StaticResource Muted}">Tags</TextBlock>
            <inputs:ComboBoxTags
                AllowAdd="True"
                IsEnabled="{Binding IsReadOnly, Converter={x:Static jonverters:BooleanFlipConverter.Default}, Mode=OneTime}"
                SelectedItems="{Binding Tags}" />
        </StackPanel>
    </Grid>
</UserControl>
