<UserControl x:Class="Automation.App.Features.History.HistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Joufflu.Controls;assembly=Joufflu"
             xmlns:fonts="clr-namespace:Joufflu.Assets.Fonts;assembly=Joufflu"
             xmlns:joufflu="clr-namespace:Joufflu;assembly=Joufflu"
             xmlns:local="clr-namespace:Automation.App.Features.History"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:HistoryViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="StateBadge" TargetType="{x:Type controls:Badge}">
            <Setter Property="Variant" Value="Default" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding State}" Value="Completed">
                    <Setter Property="Variant" Value="Success" />
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Failed">
                    <Setter Property="Variant" Value="Danger" />
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Canceled">
                    <Setter Property="Variant" Value="Secondary" />
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Progressing">
                    <Setter Property="Variant" Value="Info" />
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Waiting">
                    <Setter Property="Variant" Value="Warning" />
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Pending">
                    <Setter Property="Variant" Value="Primary" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24" joufflu:Spacing.Gap="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header + toolbar  -->
        <Grid Grid.Row="0">
            <StackPanel joufflu:Spacing.Gap="2">
                <TextBlock FontSize="20" FontWeight="SemiBold" Text="History" />
                <TextBlock Opacity="0.7" Text="Executed task instances, most recent first." />
            </StackPanel>
            <Button HorizontalAlignment="Right" VerticalAlignment="Top"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource OutlineSecondary}"
                    joufflu:ControlProperties.Size="sm">
                <StackPanel Orientation="Horizontal" joufflu:Spacing.Gap="6">
                    <fonts:FontIcon Text="{x:Static fonts:LucideFontIcons.RefreshCw}" />
                    <TextBlock Text="Refresh" />
                </StackPanel>
            </Button>
        </Grid>

        <!--  Instances  -->
        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  ItemsSource="{Binding Instances}"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="130" Header="State">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <controls:Badge Margin="4"
                                            HorizontalAlignment="Left"
                                            Content="{Binding State}"
                                            Style="{StaticResource StateBadge}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Width="*" Binding="{Binding Name}" Header="Task" />
                <DataGridTextColumn Width="180"
                                    Binding="{Binding CreatedAt, StringFormat='{}{0:g}'}"
                                    Header="Started" />
                <DataGridTextColumn Width="110" Binding="{Binding Duration}" Header="Duration" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
